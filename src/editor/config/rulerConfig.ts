// Controls the default "scale" of the ruler by setting how much real-world
// distance is visible across the viewport at the default zoom.
//
// Units: millimeters.
// Example: 2000 means "~2m visible across" at default view.
export const DEFAULT_VIEWPORT_SPAN_MM = 4000;

// The editor's world coordinates are treated as centimeters (1 world unit = 1 cm).
export const MM_PER_WORLD_UNIT = 10;
